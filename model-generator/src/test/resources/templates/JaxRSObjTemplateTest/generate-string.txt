/**
 * This code was autogenerated by ModelGenerator program. The ModelGenerator
 * creates Java Interfaces and Classes by introspecting the Cyc Knowledge Base.
 * It is strongly recommended not to make any changes to this file. 
 * This will allow regeneration of code when the Cyc KB model changes with fewer
 * downstream changes. Please implement subclasses of the generated class to add 
 * functionality.
 * 
 * <p>This code corresponds to SomeClass in the Cyc KB. 
 * 
 * <p>
 * @author ModelGenerator
 * @author Vijay Raj
 * @version "%I%, %G%"
 * @since 0.90
 */

package com.cyc.template.ws;

import com.cyc.library.restfulserviceutil.param.DateParam;
import com.cyc.library.restfulserviceutil.service.CycRestfulWS;
import com.cyc.query.Query;
import com.cyc.template.cycImpl.SomeClassImpl;
import com.cyc.template.iface.SomeClass;
import com.cyc.semantics.SCollection;
import com.cyc.semantics.Context;
import com.cyc.semantics.Fact;
import com.cyc.semantics.SIndividual;
import com.cyc.semantics.Predicate;
import java.io.File;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Date;
import java.util.List;
import javax.ws.rs.DefaultValue;
import javax.ws.rs.GET;
import javax.ws.rs.POST;
import javax.ws.rs.Path;
import javax.ws.rs.PathParam;
import javax.ws.rs.Produces;
import javax.ws.rs.QueryParam;
import javax.ws.rs.core.MediaType;
import javax.ws.rs.core.Response;

@Path("/someclass/{someclass}")
protected class SomeClassWS extends CycRestfulWS {

  @GET
  @Path("/test")
  @Produces({MediaType.APPLICATION_XML})
  public Response test(@DefaultValue("Howdy!") @QueryParam("msg") String msg) throws Exception {
    return getSerializer().response("msg", msg);
  }

  @GET
  @Path("/getGizmoObservedToBeOperationalOnDate")
  @Produces({MediaType.APPLICATION_XML})
  public Response getGizmoObservedToBeOperationalOnDate(
          @QueryParam("context") @DefaultValue("EverythingPSC") String context) {
    try {
      return getSerializer().response("result", 
              getSomeClass().getGizmoObservedToBeOperationalOnDate(new Context(context)).toString());
    } catch (Exception e) {
      return returnException(e);
    }
  }

  @GET
  @Path("/setGizmoObservedToBeOperationalOnDate")
  @Produces({MediaType.APPLICATION_XML})
  public Response setGizmoObservedToBeOperationalOnDate(
          @QueryParam("context") @DefaultValue("EverythingPSC") String context,
          @QueryParam("date") com.cyc.library.restfulserviceutil.param.DateParam date) {
    try {
      return getSerializer().response("result", 
              getSomeClass().setGizmoObservedToBeOperationalOnDate(new Context(context)).toString());
    } catch (Exception e) {
      return returnException(e);
    }
  }

  @GET
  @Path("/getGizmoObservedToBeBrokenOnDate")
  @Produces({MediaType.APPLICATION_XML})
  public Response getGizmoObservedToBeBrokenOnDate(
          @QueryParam("context") @DefaultValue("EverythingPSC") String context) {
    try {
      return getSerializer().response("result", 
              getSomeClass().getGizmoObservedToBeBrokenOnDate(new Context(context)).toString());
    } catch (Exception e) {
      return returnException(e);
    }
  }

  @GET
  @Path("/getGizmoObservedToBeBrokenOnDate_type")
  @Produces({MediaType.APPLICATION_XML})
  public Response getGizmoObservedToBeBrokenOnDate_type(
          @QueryParam("context") @DefaultValue("EverythingPSC") String context) {
    try {
      return getSerializer().response("result", 
              getSomeClass().getGizmoObservedToBeBrokenOnDate_type(new Context(context)).toString());
    } catch (Exception e) {
      return returnException(e);
    }
  }


  // Internal

  protected SomeClass getSomeClass() throws Exception {
    return new SomeClassImpl("EverythingPSC", someclass);
  }
  
  private @PathParam("someclass") String someclass;
}

